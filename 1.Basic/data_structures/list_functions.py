l1 = [1, 1, 2, 3, 4, 5]
l2 = ['a', 'b', 'c', 'd', 'e']
l3 = [6, 3, 5, 2, 1, 8, 9]

# l1.append("Krishna") # Appends the element or even other list at the end of the list

# l1.clear() # Clears all the elements in the list

# l2 = l1.copy() # Creates a copy of the original list
# print(l1, l2)

# print(l1.count(1))

# l1.extend(l2) # Extends the elements of the l2 to the l1 at the end of the l1 list. even other lists or data structures

# print(l1.index(4)) # returns the index number of the specified item

# l1.insert(2, "Bus") #  inserts the specified value at the specified position.

# l1.pop(1) # removes the element based on the given index or the last element if no index is given

# l1.remove(1)  # removes the given element in the arguments

# l1.reverse() # reverses the list

# l3.sort() # sorts the list in ascending order is reversed=True is not given
# print(l3)


print(l1)
